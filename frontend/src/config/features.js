/** 设为 true 可恢复 jumper/tunnel 连接失败时的 AI 诊断 UI */
export const AI_DEBUG_ENABLED = false

/**
 * Upgrade / 注册码兑换 UI 开关（Store 审核规避用）。
 * true  = 侧边栏隐藏 Upgrade，配置页内联展开注册码面板（默认）
 * false = 恢复旧行为：侧边栏显示 Upgrade，点击弹出 overlay 弹窗
 */
export const UPGRADE_INLINE_REDEEM = true
