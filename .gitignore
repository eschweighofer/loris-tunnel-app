#
# Build artifacts:
#   - build/bin/                ignored  (compiled binaries, not for source control)
#   - build/windows/installer/  ignored  (NSIS installer scripts generated by wails build)
#   - build/darwin/    tracked  (Info.plist, app manifests)
#   - build/windows/   tracked  (icon, manifests)
#   - build/*.png      tracked  (app icons)
#
# The two lines below un-ignore build/ in case a parent .gitignore ignores it
# (monorepo context). In a standalone repo they are harmless no-ops.
!build/
!build/**
build/bin/
build/windows/installer/

# Node / frontend outputs
node_modules/
frontend/dist
frontend/wailsjs
frontend/package.json.md5

# pnpm
.pnpm-store/
pnpm-debug.log*

# Local machine / runtime config
config.toml
ui.locale
.DS_Store

# .log
*.log

**/.vitepress/cache